# Ignore build artifacts and dependencies
node_modules/
dist/
coverage/
.mjolnir/logs/
# Machine-local state (audit R-3): counters and milestones are per-repo,
# per-machine — never committed. .mjolnir/baseline.json is deliberately
# NOT ignored: it is meant to be committed (the mjolnir.yml workflow
# fetches it from the base branch).
.mjolnir/stats.json

# FP corpus audit — cloned real-world repos, never committed
# (Bug Map M-04: also cover any rogue clone dir like .cache-kit)
tests/corpus/.cache*/

# VitePress documentation site
site/.vitepress/dist/
site/.vitepress/cache/
# Rule catalog pages — generated from docs/rules/ at build time by
# site/scripts/gen-rules.mjs; docs/rules/ is the committed source.
site/rules/

# Generated by site/scripts/gen-scan-summary.mjs; assets/readme/terminal-hero.svg
# is the committed source.
site/.vitepress/theme/generated/

# Environment
.env
.env.*

# Generated by running the README demo (examples/demo-repo)
examples/demo-repo/test-results/FLAKY.md
examples/demo-repo/test-results/TRIAGE.md

# Generated build/scan artifacts
mjolnir-badge.json
self-scan.json
# Soak drift evidence (tests/stress/soak.mjs, uploaded by stress.yml on failure)
soak-drift/


# Scratch/probe files (debug artifacts, never committed)
scratch/
probe-*

# Agent-session state (plans, worktrees, scratch notes) — not repo content,
# matching .prettierignore / mjolnir.config.json / eslint ignores.
.kilo/
package/

# Saved HTML artifact exports dropped into .planning for reference
.planning/
.planning/*_files/
.planning/CRITIQUE-REMEDIATION-PLAN.md
.planning/M1-M2-TASKS.md
.planning/ROADMAP-2026.md
*.log
.vscode/
