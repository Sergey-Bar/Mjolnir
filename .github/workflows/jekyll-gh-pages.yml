name: Legacy Pages Compatibility Check

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-pages-disabled:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Confirm canonical Pages publisher
        run: echo "Deployment is owned exclusively by .github/workflows/pages.yml"
