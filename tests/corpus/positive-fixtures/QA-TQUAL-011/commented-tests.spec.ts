import { describe, it } from "vitest";

// it("old login flow", () => { ... });

describe("payments", () => {
  // it("charges the card", () => { ... });
  // it("issues refunds", () => { ... });

  it("renders invoice", () => {});
});

// it("legacy export", () => { ... });
// it("legacy import", () => { ... });
// it("deprecated sync", () => { ... });
