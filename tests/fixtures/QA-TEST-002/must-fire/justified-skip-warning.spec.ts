describe("billing", () => {
  // Skipped because of https://github.com/acme/app/issues/412
  it.skip("charges the card", () => {
    expect(true).toBe(true);
  });

  it.skip("refunds the card", () => {
    expect(true).toBe(true); // reason: blocked by PROJ-99
  });
});
