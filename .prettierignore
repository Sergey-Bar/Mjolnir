dist/
coverage/
node_modules/
package-lock.json
scratch/

# Fixture corpora are DATA (§08 class-B/C exhibits) — deliberately contain
# anti-patterns; never reformatted.
tests/corpus/positive-fixtures/
tests/corpus/negative-fixtures/

# Rule catalog pages generated from docs/rules/ by site/scripts/gen-rules.mjs
site/rules/

# Saved HTML artifact exports (planning reference material, not source)
.planning/**/*.html

# Generated build/scan artifacts — mirrors .gitignore so `prettier --check .`
# is order-independent (M-05): these exist only after a local scan/build and
# match the lint-staged contract.
mjolnir-badge.json
self-scan.json
# Soak drift evidence (tests/stress/soak.mjs, uploaded by stress.yml on failure)
soak-drift/

# Agent-session state (plans, scratch notes) — not repo content
.kilo/

# Committed video scripts — generated by `npm run docs:video:capture` and
# locked byte-for-byte by tests/contract/video-script.spec.ts. Prettier
# collapses their short arrays, which would rewrite the file the contract
# compares against and break the lock on every format run.
assets/video/script.*.json
