{
  "name": "mjolnir-site",
  "private": true,
  "type": "module",
  "scripts": {
    "gen": "node scripts/gen-rules.mjs",
    "predev": "npm run gen",
    "dev": "vitepress dev",
    "prebuild": "npm run gen",
    "build": "vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
