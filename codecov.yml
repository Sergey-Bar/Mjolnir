coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 2%
    patch:
      default:
        target: 70%
        threshold: 5%
  precision: 1
  round: down

comment:
  layout: "reach,diff,flags,files"
  behavior: default
  require_changes: false
