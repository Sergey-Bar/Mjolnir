{
  "schemaVersion": 1,
  "id": "tour",
  "normalization": [
    "/· (?:\\d+ms|(?:0\\.\\d*[1-9]\\d*|[1-9]\\d*(?:\\.\\d+)?)s)$/ → '· a few ms'"
  ],
  "environment": {
    "reporterWidth": 92,
    "viewport": [
      1280,
      720
    ],
    "deviceScaleFactor": 2,
    "fps": 30,
    "fonts": [
      "GeistMono-Regular.ttf",
      "GeistMono-Bold.ttf",
      "FreeMono.ttf"
    ]
  },
  "beats": [
    {
      "id": "tour-scan",
      "narrative": "The full report: score, category breakdown, and every finding.",
      "source": "examples/demo-repo",
      "command": "npx qa-doctor-cli@latest --verbose",
      "ansi": [
        "  Q A  D O C T O R  ·  VERIFICATION TRUST ENGINE",
        "",
        "",
        "  TEST HEALTH  80/100  HEALTHY",
        "  ███████████████████████▓░░░░░░",
        "  Healthy test health: 18 findings remain.",
        "  (32 raw pts / 7 test declarations — normalized)",
        "  (capped: deduction mass 32 pts — absolute ceiling 95)",
        "",
        "  DETECTED [playwright]",
        "",
        "  ▍ DIAGNOSTICS BY CATEGORY",
        "  QA-CI    ███████████▓░░░░  76",
        "  QA-PW    ██████████████▓░  95",
        "  QA-TEST  ████████████████  97",
        "",
        "  ▍ WHERE POINTS WERE LOST",
        "  ╭─────────────────────────────────────────╮",
        "  │ 3 × error   − 24                        │",
        "  │ 3 × warning −  7 (evidence-discounted)  │",
        "  │ 12 × info    −  1 (evidence-discounted) │",
        "  ╰─────────────────────────────────────────╯",
        "",
        "  ▍ FIX THIS FIRST",
        "  +8 pts  QA-CI-009 · .github/workflows/ci.yml:10",
        "  +8 pts  QA-CI-009 · .github/workflows/ci.yml:10",
        "  +8 pts  QA-CI-009 · .github/workflows/ci.yml:26",
        "",
        "  ▍ FINDINGS",
        "",
        "  ✗ ERROR   QA-CI-009 · .github/workflows/ci.yml:10",
        "    [E2 · deterministic · measured FP 0% · n=10]",
        "    Finding Job `test` pipes the test command into another tool without `set -o pipefail`.",
        "    Impact  FALSE-GREEN — In a shell pipeline only the last command's exit code counts — a",
        "            failing test run is masked by the downstream tool succeeding.",
        "    Fix     Add `shell: bash` with `set -o pipefail`, or split into two steps so the test",
        "            command's exit code is preserved.",
        "    Verify  Re-run qa-doctor after the change — the gate should stop failing and the score",
        "            should recover by 8.",
        "",
        "  ✗ ERROR   QA-CI-009 · .github/workflows/ci.yml:10",
        "    [E2 · deterministic · measured FP 0% · n=10]",
        "    Finding Job `deploy-smoke` sequences commands with `; ` after the test command — the",
        "            test result does not fail the step.",
        "    Impact  FALSE-GREEN — With `;` sequencing the step's exit status comes from the last",
        "            command only, so failed tests still yield a green checkmark.",
        "    Fix     Chain with `&&` instead of `;` so a test failure fails the step.",
        "    Verify  Re-run qa-doctor after the change — the gate should stop failing and the score",
        "            should recover by 8.",
        "",
        "  ℹ INFO    QA-CI-010 · .github/workflows/ci.yml:21",
        "    [E0 · observation · measured FP 10% · n=10]",
        "    Finding Job `deploy-smoke` runs tests but its `if:` condition skips it on pull",
        "            requests.",
        "    Impact  FALSE-GREEN — If this job is a required check, PRs can satisfy it while zero",
        "            tests executed — the green checkmark verifies nothing.",
        "    Fix     Remove the skip-on-PR condition, or split into a dedicated PR test job and",
        "            require THAT one in branch protection.",
        "    Verify  Re-run qa-doctor after the change — the finding should no longer appear.",
        "",
        "  ✗ ERROR   QA-CI-009 · .github/workflows/ci.yml:26",
        "    [E2 · deterministic · measured FP 0% · n=10]",
        "    Finding Job `deploy-smoke` pipes the test command into another tool without `set -o",
        "            pipefail`.",
        "    Impact  FALSE-GREEN — In a shell pipeline only the last command's exit code counts — a",
        "            failing test run is masked by the downstream tool succeeding.",
        "    Fix     Add `shell: bash` with `set -o pipefail`, or split into two steps so the test",
        "            command's exit code is preserved.",
        "    Verify  Re-run qa-doctor after the change — the gate should stop failing and the score",
        "            should recover by 8.",
        "",
        "  ℹ INFO    QA-CI-001 · .github/workflows/ci.yml:27",
        "    [E0 · observation · measured FP 11% · n=19]",
        "    Finding Job `security-scan` runs a verification gate under `continue-on-error: true`.",
        "    Impact  FALSE-GREEN — This job can fail every day and CI will still show green. The",
        "            checkmark on this workflow cannot be trusted.",
        "    Fix     Remove continue-on-error, or scope it to individual non-blocking steps only.",
        "    Verify  Re-run qa-doctor after the change — the finding should no longer appear.",
        "",
        "  ℹ INFO    QA-PW-146 · e2e/checkout.spec.ts:5",
        "    [E0 · observation · measured FP 12% · n=17 · trust L3 · runtime: file executed]",
        "    Finding CSS/XPath string selector where a normalized locator is expected.",
        "    Impact  HYGIENE — String CSS/XPath selectors couple the test to markup the user never",
        "            sees — they break on any refactor and can silently select the wrong element",
        "            after a redesign.",
        "    Fix     Normalize to Playwright's user-facing getters: getByRole('button', { name: '…'",
        "            }), getByLabel('…'), getByTestId('…'), or getByText('…') — they survive markup",
        "            refactors and match what a user actually perceives.",
        "    Verify  Re-run qa-doctor after the change — the finding should no longer appear.",
        "",
        "  ℹ INFO    QA-PW-004 · e2e/checkout.spec.ts:5",
        "    [E0 · observation · measured FP 43% · n=14 · trust L3 · runtime: file executed]",
        "    Finding Brittle multi-class CSS selector: `locator(\".btn.btn-primary >",
        "            div:nth-child(2)\")`.",
        "    Impact  HYGIENE — Structural selectors break on any DOM refactor and fail without",
        "            telling you which behavior regressed.",
        "    Fix     Prefer role-based locators: getByRole(), getByText(), getByLabel().",
        "    Verify  Re-run qa-doctor after the change — the finding should no longer appear.",
        "",
        "  ℹ INFO    QA-PW-146 · e2e/checkout.spec.ts:13",
        "    [E0 · observation · measured FP 12% · n=17 · trust L3 · runtime: file executed]",
        "    Finding CSS/XPath string selector where a normalized locator is expected.",
        "    Impact  HYGIENE — String CSS/XPath selectors couple the test to markup the user never",
        "            sees — they break on any refactor and can silently select the wrong element",
        "            after a redesign.",
        "    Fix     Normalize to Playwright's user-facing getters: getByRole('button', { name: '…'",
        "            }), getByLabel('…'), getByTestId('…'), or getByText('…') — they survive markup",
        "            refactors and match what a user actually perceives.",
        "    Verify  Re-run qa-doctor after the change — the finding should no longer appear.",
        "",
        "  ℹ INFO    QA-TEST-002 · e2e/checkout.spec.ts:16",
        "    [E0 · observation · measured FP 62% · n=21 · trust L3 · runtime: file executed]",
        "    Finding Skipped test without justification: `test.skip(`.",
        "    Impact  FALSE-GREEN — Skipped tests hide broken or unimplemented behavior behind a",
        "            green checkmark.",
        "    Fix     Fix and re-enable the test, or delete it with a tracked issue reference.",
        "    Verify  Re-run qa-doctor after the change — the finding should no longer appear.",
        "",
        "  ℹ INFO    QA-PW-123 · e2e/login.spec.ts:4",
        "    [E0 · observation · measured FP 46% · n=11]",
        "    Finding Hardcoded URL: `goto(\"https://staging.example.com/login\"`.",
        "    Impact  HYGIENE — Specs pointing at absolute URLs break when environments change and",
        "            can hit production by accident.",
        "    Fix     Use relative paths with baseURL from playwright.config, or an env variable.",
        "    Verify  Re-run qa-doctor after the change — the finding should no longer appear.",
        "",
        "  ℹ INFO    QA-PW-146 · e2e/login.spec.ts:6",
        "    [E0 · observation · measured FP 12% · n=17]",
        "    Finding CSS/XPath string selector where a normalized locator is expected.",
        "    Impact  HYGIENE — String CSS/XPath selectors couple the test to markup the user never",
        "            sees — they break on any refactor and can silently select the wrong element",
        "            after a redesign.",
        "    Fix     Normalize to Playwright's user-facing getters: getByRole('button', { name: '…'",
        "            }), getByLabel('…'), getByTestId('…'), or getByText('…') — they survive markup",
        "            refactors and match what a user actually perceives.",
        "    Verify  Re-run qa-doctor after the change — the finding should no longer appear.",
        "",
        "  ℹ INFO    QA-PW-123 · e2e/login.spec.ts:11",
        "    [E0 · observation · measured FP 46% · n=11]",
        "    Finding Hardcoded URL: `goto(\"https://staging.example.com/\"`.",
        "    Impact  HYGIENE — Specs pointing at absolute URLs break when environments change and",
        "            can hit production by accident.",
        "    Fix     Use relative paths with baseURL from playwright.config, or an env variable.",
        "    Verify  Re-run qa-doctor after the change — the finding should no longer appear.",
        "",
        "  ⚠ WARN    QA-PW-122 · playwright.config.ts:1",
        "    [E2 · deterministic · measured FP 6% · n=80]",
        "    Finding playwright.config has no `trace` capture setting.",
        "    Impact  FALSE-GREEN — A test that fails once and passes on retry is exactly the case",
        "            you'll need evidence for later — with no trace, the flake is uninvestigable.",
        "    Fix     Add `use: { trace: 'on-first-retry' }` to the config.",
        "    Verify  Re-run qa-doctor after the change — deduction should drop by 3.",
        "",
        "  ℹ INFO    QA-PW-143 · playwright.config.ts:1",
        "    [E2 · deterministic · measured FP 6% · n=80]",
        "    Finding playwright.config captures neither screenshots nor video on failure.",
        "    Impact  HYGIENE — Once the CI runner is gone, a failed UI test is just a stack trace.",
        "            Screenshots/video on failure turn 'cannot reproduce' into a five-second",
        "            diagnosis.",
        "    Fix     Add `use: { screenshot: 'only-on-failure', video: 'retain-on-failure' }` to",
        "            the config.",
        "    Verify  Re-run qa-doctor after the change — the finding should no longer appear.",
        "",
        "  ⚠ WARN    QA-PW-141 · playwright.config.ts:5",
        "    [E1 · heuristic · measured FP 9% · n=33]",
        "    Finding retries: undefined with no visible flake-triage loop.",
        "    Impact  FALSE-GREEN — Retries convert intermittent failures into silent passes.",
        "            Without a forensics/triage step consuming retry data, flaky tests pass forever",
        "            and real regressions hide behind lucky reruns.",
        "    Fix     Keep retries <= 2 and feed retry outcomes into `qa-doctor forensics`/`triage`,",
        "            or add a reporter so flaky passes are reviewed.",
        "    Verify  Re-run qa-doctor after the change — deduction should drop by 1.",
        "",
        "  ℹ INFO    QA-PY-003 · tests/test_checkout_api.py:6",
        "    [E0 · observation · measured FP 47% · n=30]",
        "    Finding Test `test_checkout_endpoint_responds` contains no assertions.",
        "    Impact  FALSE-GREEN — Without an assertion the test can only fail by crashing — it",
        "            cannot detect behavioral regressions.",
        "    Fix     Add an `assert` on the expected outcome, or remove the test.",
        "    Verify  Re-run qa-doctor after the change — the finding should no longer appear.",
        "",
        "  ⚠ WARN    QA-PY-005 · tests/test_checkout_api.py:7",
        "    [E2 · deterministic · measured FP 13% · n=23]",
        "    Finding Hard sleep: `time.sleep(2…`.",
        "    Impact  FLAKY-RISK — Fixed sleeps make tests slow and flaky — they guess at timing",
        "            instead of waiting for state.",
        "    Fix     Wait for an explicit condition (polling helper, pytest-timeout wait_until, or",
        "            event).",
        "    Verify  Re-run qa-doctor after the change — deduction should drop by 3.",
        "",
        "  ℹ INFO    QA-PY-003 · tests/test_checkout_api.py:12",
        "    [E0 · observation · measured FP 47% · n=30]",
        "    Finding Test `test_refund_endpoint_accepts_request` contains no assertions.",
        "    Impact  FALSE-GREEN — Without an assertion the test can only fail by crashing — it",
        "            cannot detect behavioral regressions.",
        "    Fix     Add an `assert` on the expected outcome, or remove the test.",
        "    Verify  Re-run qa-doctor after the change — the finding should no longer appear.",
        "",
        "",
        "──────────────────────────────────────────────────────────",
        "  Analysis: complete · a few ms",
        "  11 advisory findings (E0 — observation only, no score impact)",
        "  Rule coverage: 12/12 rules that fired here have a measured false-positive rate; the rest",
        "  are heuristics. `qa-doctor rules --unmeasured` lists them.",
        "  Scope: PARTIAL — unrecognized:2; analyzed 5 of 5 discovered files. No repository-verified",
        "  claim applies to this scan.",
        "  Runtime evidence: 4/18 findings corroborated by a real run report (trust L3–L5); the rest",
        "  are static-only.",
        ""
      ],
      "assertions": {
        "requiredFindings": [
          "QA-CI-009"
        ],
        "score": 80,
        "errorCount": 3,
        "findingCount": 18
      }
    },
    {
      "id": "tour-explain",
      "narrative": "One rule, up close — what it found, why it matters, how to fix it, and whether its false-positive rate has been measured.",
      "source": "tests/fixtures",
      "command": "qa-doctor explain QA-CI-009",
      "ansi": [
        "  ▍ QA-CI-009 — Test command does not propagate exit code",
        "",
        "Severity:    error",
        "Confidence:  high",
        "Tier:        extended",
        "Evidence:    E2",
        "QA impact:   False-green risk (FALSE-GREEN)",
        "Measured FP: 0% (10 hand-classified corpus verdicts)",
        "FP risk:     low (author estimate)",
        "Languages:   yaml, groovy",
        "Frameworks:  github-actions, jenkins",
        "",
        "WHAT WAS FOUND (real detector output, not a mockup)",
        "  Job `test` pipes the test command into another tool without `set -o pipefail`.",
        "",
        "WHY IT MATTERS",
        "  In a shell pipeline only the last command's exit code counts — a failing test",
        "  run is masked by the downstream tool succeeding.",
        "",
        "HOW TO FIX",
        "  Add `shell: bash` with `set -o pipefail`, or split into two steps so the test",
        "  command's exit code is preserved.",
        "",
        "  Example from this rule's own must-fire fixture:",
        "  QA-CI-009/must-fire/masked-pipe.yml",
        "",
        "WHAT WOULD CHANGE THE VERDICT",
        "  - a run report next to the scan target (qa-doctor.report.json or",
        "  test-results/) corroborating this file lifts its findings to L3–L5",
        "  - a documented suppression (qa-doctor.config.json) lowers the finding count",
        "  without claiming correctness",
        "",
        "NEXT ACTION",
        "  Fix the first occurrence, then re-run: `qa-doctor --scope changed`. Every",
        "  occurrence of this rule is listed in the scan output.",
        "",
        "HOW TO VERIFY THE FIX",
        "  Re-run `qa-doctor` on the changed file(s) — this finding should no longer",
        "  appear. `qa-doctor --scope changed` scopes the check to just what you touched.",
        "",
        "Docs: qa-doctor rules --md   (full catalog, this rule included)"
      ]
    },
    {
      "id": "tour-forensics",
      "narrative": "Real run data, not static analysis: a test that only passed on attempt two is a lucky test, and gets labelled TRUE-FLAKE.",
      "source": "examples/demo-repo/test-results",
      "command": "qa-doctor forensics ./test-results/",
      "ansi": [
        "  ▍ FLAKINESS LEADERBOARD",
        "",
        "3 tests · 1 failed · 1 flaky · 1 retried",
        "",
        "TRUE-FLAKE completes checkout with saved card (e2e/checkout.spec.ts)",
        "           ████████████████████ 6.0s · 2 attempts",
        "FAILING    declines an expired card (e2e/checkout.spec.ts)",
        "           ████░░░░░░░░░░░░░░░░ 1.1s · 1 attempt",
        "",
        "FLAKY.md not written (--no-flaky-md)."
      ]
    }
  ]
}
