{
  "exclude": [
    "tests/fixtures/**",
    "tests/golden/**",
    "examples/**",
    "scratch/**"
  ],
  "ignore": [
    {
      "ruleId": "QA-TEST-003",
      "files": [
        "tests/adapter-deep-branch-coverage.spec.ts",
        "tests/contract-schema.spec.ts",
        "tests/cross-file.spec.ts",
        "tests/fix.spec.ts",
        "tests/fuzz.spec.ts",
        "tests/impact.spec.ts",
        "tests/mermaid-format.spec.ts",
        "tests/rule-branch-coverage-misc.spec.ts",
        "tests/rule-metadata-lint.spec.ts",
        "tests/sprint6-cli-commands.spec.ts"
      ],
      "reason": "Self-scan dogfooding: these files embed test-like patterns (it/test with empty bodies) inside string literals as fixture data for the scanner — not real assertion-less tests."
    },
    {
      "ruleId": "QA-TEST-010",
      "files": [
        "tests/adapter-deep-branch-coverage.spec.ts",
        "tests/cross-file.spec.ts",
        "tests/fix.spec.ts",
        "tests/impact.spec.ts",
        "tests/rule-metadata-lint.spec.ts",
        "tests/sprint6-cli-commands.spec.ts"
      ],
      "reason": "Self-scan dogfooding: these files embed test-like patterns (it/test with empty arrow bodies) inside string literals as fixture data — not real empty tests."
    },
    {
      "ruleId": "QA-TQUAL-002",
      "files": ["tests/reporters.spec.ts"],
      "reason": "Intentional placeholder guard: the selector-health describe block uses expect(true).toBe(true) to keep vitest from warning about an empty suite."
    },
    {
      "ruleId": "QA-TEST-002",
      "files": ["tests/registry-install.spec.ts"],
      "reason": "Intentionally skipped: the registry-install smoke test is gated behind RUN_REGISTRY_INSTALL_TEST=1 because it requires network and is slow."
    },
    {
      "ruleId": "QA-PW-119",
      "files": ["tests/registry-install.spec.ts"],
      "reason": "The registry-install smoke test uses beforeAll/afterAll with shared mutable state (workDir, installDir) by design — it creates and destroys a real npm install directory, which is inherently stateful."
    },
    {
      "ruleId": "QA-CI-002",
      "files": [".github/workflows/qa-doctor.yml"],
      "reason": "The 'Restore baseline from base branch' step's `git checkout ... || echo` is a genuine best-effort fallback for an OPTIONAL file — not a test command whose failure is being hidden."
    }
  ]
}
