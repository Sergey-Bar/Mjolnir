{
  "exclude": ["tests/fixtures/**", "tests/golden/**", "examples/**"],
  "ignore": [
    {
      "ruleId": "QA-PW-101",
      "files": ["tests/**"],
      "reason": "Self-scan dogfooding: this repo's own test suite embeds sample Playwright code inside template-literal strings. The waitForTimeout occurrences are fixture payloads for testing the scanner itself, not real hard sleeps."
    },
    {
      "ruleId": "QA-PW-140",
      "files": ["tests/**"],
      "reason": "Self-scan dogfooding: screenshot-name literals inside string-embedded test snippets trigger the diff-tolerance rule."
    },
    {
      "ruleId": "QA-TQUAL-002",
      "files": ["tests/**"],
      "reason": "Self-scan dogfooding: string-embedded sample code (expect(1).toBe(1) payloads written to temp fixture files by the tests themselves)."
    },
    {
      "ruleId": "QA-TEST-001",
      "files": ["tests/**"],
      "reason": "Self-scan dogfooding: it.only/test.only literals embedded in test strings to exercise the focused-test rule."
    },
    {
      "ruleId": "QA-TEST-003",
      "files": ["tests/**"],
      "reason": "Self-scan dogfooding: assertion-less sample bodies embedded in test strings."
    },
    {
      "ruleId": "QA-PW-003",
      "files": ["tests/**"],
      "reason": "Self-scan dogfooding: page.evaluate samples embedded in test strings."
    },
    {
      "ruleId": "QA-TEST-010",
      "files": ["tests/**"],
      "reason": "Self-scan dogfooding: empty-body samples embedded in test strings."
    },
    {
      "ruleId": "QA-PW-002",
      "files": ["tests/**"],
      "reason": "Self-scan dogfooding: unawaited-assertion samples embedded in test strings."
    },
    {
      "ruleId": "QA-TQUAL-009",
      "files": ["tests/**"],
      "reason": "Self-scan dogfooding: promise-assertion samples embedded in test strings."
    },
    {
      "ruleId": "QA-TEST-002",
      "files": ["tests/**"],
      "reason": "Self-scan dogfooding: unjustified-skip samples are test data, not real skipped tests."
    },
    {
      "ruleId": "QA-PW-119",
      "files": ["tests/registry-install.spec.ts"],
      "reason": "The registry-install smoke test uses beforeAll/afterAll with shared mutable state (workDir, installDir) by design — it creates and destroys a real npm install directory, which is inherently stateful."
    },
    {
      "ruleId": "QA-CI-002",
      "files": [".github/workflows/qa-doctor.yml"],
      "reason": "The 'Restore baseline from base branch' step's `git checkout ... || echo` is a genuine best-effort fallback for an OPTIONAL file — not a test command whose failure is being hidden."
    }
  ]
}
