{
  "exclude": [
    "tests/fixtures/**",
    "tests/golden/**",
    "examples/**",
    "scratch/**"
  ],
  "ignore": [
    {
      "ruleId": "QA-TEST-003",
      "files": [
        "tests/contract-schema.spec.ts",
        "tests/mermaid-format.spec.ts"
      ],
      "reason": "These tests assert via helper functions that throw on failure (assertScanResultShape, validateFlowchartStructure) — a valid pattern that the regex heuristic cannot recognize."
    },
    {
      "ruleId": "QA-TQUAL-002",
      "files": ["tests/reporters.spec.ts"],
      "reason": "Intentional placeholder guard: the selector-health describe block uses expect(true).toBe(true) to keep vitest from warning about an empty suite."
    },
    {
      "ruleId": "QA-TEST-002",
      "files": ["tests/registry-install.spec.ts"],
      "reason": "Intentionally skipped: the registry-install smoke test is gated behind RUN_REGISTRY_INSTALL_TEST=1 because it requires network and is slow."
    },
    {
      "ruleId": "QA-CI-001",
      "files": [".github/workflows/eslint.yml"],
      "reason": "GitHub's ESLint SARIF template: `continue-on-error: true` on the lint step is intentional so `upload-sarif` still runs when findings exist. This job is advisory (Security tab reporting) — the blocking lint gate is `npm run lint` in ci.yml."
    }
  ]
}
